# Log 2026-06-18

## Sesión — Bot de Telegram para post-venta MercadoLibre

Decisión de arquitectura: bot TODO dentro del Worker `mlpu-proxy` (Opción A) —
webhook + Cron Trigger + KV, sin servidor Node ni BD nuevos. El prompt original asumía
Node/Express+Telegraf+SQLite; se corrigió al stack real (Cloudflare Worker serverless).
Trabajo additivo, módulos nuevos separados. Pasos 3-4-5 con flujo de evidencia por
mensajería ML. Paso 4 hecho con 2 subagentes builder en paralelo (messaging.js + scheduler.js).

### Paso 1 — base bot
- `worker/telegram-bot.js` (nuevo): tgApi/tgSend, webhook `POST /tg/webhook`, /start.
- `index.js` (additivo): import + ruta /tg/webhook antes del guard /ml/.
- Secret TELEGRAM_BOT_TOKEN. Webhook seteado. Probado: /start devuelve chat id 1036420688.

### Paso 2 — webhook ML + alerta venta
- `worker/orders.js` (nuevo): KV ML_ORDERS (getOrder/saveOrder/listOrders + índice
  orders:index), recordOrderFromML (alerta a Telegram si status==='paid', idempotente).
- `index.js`: ctx en fetch; ruta `POST /ml/notifications` (orders_v2/shipments) →
  handleMlNotification → GET orden+shipment → recordOrderFromML, en ctx.waitUntil (200 ya).
- KV ML_ORDERS creado (id d72bf3...); binding en wrangler.toml. Secret TELEGRAM_CHAT_ID.

### Paso 3 — foto sticker → tracking
- `worker/tracking.js` (nuevo): extractTrackingFromImage (Anthropic claude-sonnet-4-6),
  findPendingMatches/clearMatch (match por nombre normalizado), assignTracking (PUT
  /shipments best-effort + persiste tracking+carrier en KV).
- `telegram-bot.js`: manejo de fotos, /si /no /asignar, estado pending:<chatId> (TTL 1h),
  tgGetFileBytes. Secret ANTHROPIC_API_KEY.

### Evidencia (tracking + fotos del empaque por mensajería ML)
- `worker/messaging.js` (nuevo, subagente): uploadAttachment (multipart a
  /messages/attachments?tag=post_sale&site_id=MLC), sendBuyerMessage, sendBuyerEvidence.
- `tracking.js`: assignTracking ya NO manda mensaje (lo hace el flujo evidencia).
- `telegram-bot.js`: tras /si → recolecta fotos → /listo → sendBuyerEvidence (tracking +
  fotos en un mensaje). tgGetFileBuffer (ArrayBuffer) para subir adjuntos.

### Paso 4 — seguimiento automático
- `worker/scheduler.js` (nuevo, subagente): runScheduled — recorre órdenes con tracking,
  GET /shipments/:id; shipped→"en camino", delivered→registra, delivered+48h→feedback
  positivo + recordatorio reseña (Instagram @topwheels.cl + subir a historias). Flags:
  shipped_notified, delivered_at, delivered_notified, feedback_done.
- `index.js`: handler scheduled() → ctx.waitUntil(runScheduled). wrangler.toml:
  [triggers] crons = ["0 */6 * * *"].

### Paso 5 — comandos del vendedor
- `telegram-bot.js`: /ordenes, /pendientes, /orden <id>, /mensaje <id> <texto>
  (reusa sendBuyerEvidence). /start lista comandos. setMyCommands registrado (ok).

### Verificación
- esbuild --bundle sin errores en cada deploy. Worker desplegado (Version 619008a4).
- /tg/webhook responde "ok"; /ml/notifications simulado responde {"ok":true}.

### Pendiente / próximo
- USUARIO: configurar callback ML (URL /ml/notifications + topics) en app 3829359465845583.
- Prueba real con wrangler tail para validar endpoints ML (tracking/attachments/msg/feedback).
- Worker NO commiteado aún (solo working tree).
- Seguridad: TELEGRAM_BOT_TOKEN quedó pegado en chat → considerar rotarlo; .env en OneDrive.

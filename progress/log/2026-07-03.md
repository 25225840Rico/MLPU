# 2026-07-03 — Sesión 7: chequeo general, bot fuera de servicio, envío pagado

## 1. Chequeo al reanudar ("entra acá y chequea todo")
- Repo `automl` (MLPU): main al día con origin, último commit 17ea095 (sesión 6).
  Solo `progress/` estaba sin commitear (cierre de sesión 6 no commiteado).
- Worker vivo; webhook Telegram OK (url correcta, 0 pendientes).
- **KV ML_ORDERS**: 8 órdenes + `orders:index` — tras la limpieza del 06-19 las
  ventas nuevas SÍ entraron solas → las notificaciones `orders_v2 →
  /ml/notifications` FUNCIONAN. El pendiente C de la sesión 6 quedó cerrado por
  evidencia. Orden más nueva: 2000017167156568 (Rubén Nass, 06-29, Hot Wheels
  Tacoma $12.990) con `ig_cta_sent=false / ig_cta_warned=true` → el bloqueo de
  ML al vendedor NEWBIE seguía activo a esa fecha.
- KV ML_TOKENS: `ml_session` con access/refresh (token válido, users/me →
  GUAR8622673).

## 2. Bot fuera de servicio (pedido explícito del usuario)
Enfoque reversible: NO se borra el Worker (sigue recibiendo notificaciones ML y
refrescando OAuth on-demand), solo se apagan las dos entradas del bot:
- `worker/wrangler.toml`: `crons = []` (antes `*/10 * * * *`). El deploy con
  lista vacía des-registra el cron trigger en Cloudflare.
- `worker/index.js` (`handleTgAdmin`): nueva acción `off` →
  `POST deleteWebhook {drop_pending_updates:true}` usando el secret del Worker
  (no había token de Telegram local; el admin endpoint solo tenía info/set/catchup).
- Deploy Version **6c598763** → `GET /tg/admin?action=off` →
  `{"ok":true,"description":"Webhook was deleted"}` → verificado con
  `action=info`: `url: ""`.
- Commit **d813d66** pusheado a main.
- Reactivación futura: restaurar crons en wrangler.toml + `npx wrangler deploy`
  + `GET /tg/admin?action=set`.

## 3. Inventario: envío gratis → pagado por el cliente
Pedido: cambiar TODO el inventario de envío gratis a que lo pague el cliente.
El usuario eligió (AskUserQuestion) aplicar a **todas** (146), no piloto.

Método (scripts Python con urllib, token desde KV `ml_session`, guardados en el
scratchpad de la sesión, no en el repo):
1. `GET /users/283388639/items/search` paginado → 149 IDs.
2. `GET /items?ids=...&attributes=id,title,price,status,shipping,...` en lotes
   de 20 → snapshot en `scratchpad/items.json`.
   - 138 active / 6 paused / 5 under_review. 146 con `free_shipping=true`,
     3 ya en pagado (MLC4044510890, MLC4044406402, MLC1984838327).
   - Todas `mode=me2`, `logistic_type=drop_off`.
3. PUT `/items/{id}` con
   `{"shipping":{"mode":"me2","local_pick_up":<actual>,"free_shipping":false}}`,
   secuencial, sleep 0.25 s, reintento 429/503 con backoff.

Resultado (guardado en `scratchpad/shipping_result.json`):
- **106 cambiadas OK** (free_shipping=false confirmado en la respuesta).
- **40 "rechazadas"**: 39 devolvieron HTTP 200 pero ML mantuvo
  `free_shipping=true` EN SILENCIO; 1 devolvió 400
  `field_not_updatable (shipping.free_shipping, cause_id 240)`.
  TODAS las rechazadas tienen precio ≥ $19.990 → umbral de envío gratis
  obligatorio de ML Chile.
- Verificación final re-leyendo la API completa: **109 pagado / 40 gratis**;
  mínimo precio con gratis = $19.990.
- Matiz: algunos ítems caros (hasta $45.000) SÍ aceptaron el cambio — la regla
  no es puramente por precio (categoría/flag por publicación). No profundizado.

## Gotchas técnicos de la sesión
- `wrangler kv key get` mete "npm notice" en stdout → filtrar con grep.
- Consola cp1252: `print` con caracteres como '→' revienta en Python; usar
  `sys.stdout.reconfigure(encoding='utf-8')`.
- ML puede responder 200 a un PUT y aun así ignorar `free_shipping` — SIEMPRE
  verificar el valor devuelto/re-leído, no el status code.

## Estado al cierre
- main = d813d66 (pusheado). Working tree: solo progress/ (este checkpoint).
- Bot: SIN webhook, SIN cron. Worker desplegado (Version 6c598763).
- Inventario: 109/149 con envío a cargo del cliente; 40 bloqueadas por ML.

## 4. (Continuación) Rebaja de precios + Excel de inventario
- De las 40 con envío gratis forzado, el usuario eligió bajar SOLO las ≤ $20.990
  (resultaron 20, no 22): todas a $19.980 + free_shipping=false.
- 12 OK directo. 8 fallaron con `item.price.not_modifiable` (cause_id 339):
  tienen VARIACIONES → el precio se actualiza vía
  `PUT {variations:[{id, price}], shipping:{...}}`. Con eso: 8/8 OK.
- Verificación final API completa: 149 items → **129 envío pagado / 20 gratis**
  (las 20 caras ≥ $25.990 que se decidió no rebajar); 20 items quedaron a $19.980.
- Excel generado: `inventario_ML_2026-07-03.xlsx` en la raíz de automl
  (hoja Inventario: ID, título, precio, estado, envío, stock, vendidos, tipo,
  categoría, link, filas amarillas = envío gratis; hoja Resumen con totales).
  openpyxl instalado (3.1.5). Snapshot crudo en scratchpad/items_final.json.

## 5. Stock reducido a 1 por producto
- 69 publicaciones tenían stock > 1 (62 con 3, 7 con 2). PUT available_quantity=1
  (fallback vía variations donde aplicó). 68/69 OK.
- Única fallida: MLC1986993239 (Subaru Impreza, under_review) —
  field_not_updatable mientras está en revisión; reintentar cuando salga.
- Las 5 con stock 0 se dejaron en 0 (no inventar stock).
- Estado final verificado: 143 con stock 1 · 5 con 0 · 1 con 3 (la en revisión).
- Excel regenerado con los datos finales (mismo archivo inventario_ML_2026-07-03.xlsx;
  Resumen ahora incluye conteo de stock).

## 6. Módulo de catalogado en el bot (reemplazo del SPA AutoPublisher)
Pedido: llevar las funciones del SPA al bot de Telegram, para que 2 personas
(él + esposa) cataloguen en paralelo desde sus celulares hacia LA MISMA cuenta
de ML, todo con envío a cargo del comprador. Commit ce3a59e, Version b67b83e1.

- worker/publisher.js (NUEVO): puerto fiel de src/agents/orchestrator.js +
  App.jsx publish(): analyzeProduct (4 asks paralelos a claude-haiku-4-5),
  discoverCategories (domain_discovery MLC), getRequiredAttrs +
  fillAttributesWithAI, getMarketPrices (search por categoría), uploadPicture
  (multipart a /pictures/items/upload), createListing (payload igual al SPA
  pero con shipping SIEMPRE free_shipping=false y available_quantity SIEMPRE 1,
  listing_type free, garantía 30 días; descripción por endpoint aparte).
- telegram-bot.js: fotos → flujo catalogado por defecto (acumula file_ids en
  pending:<chatId>, mode cat_photos → cat_confirm). Sticker de despacho quedó
  detrás del botón "📦 Despacho" (mode await_sticker). Vista previa con botones:
  Publicar / Precio / Título / Categoría (top 5 con recálculo de atributos) /
  Nuevo↔Usado / Cancelar. Si ML rechaza el POST, el borrador queda para corregir
  y Reintentar. MAIN_KB ampliado (📸 Catalogar, 📦 Despacho).
- Control de acceso: isAllowed (admin TELEGRAM_CHAT_ID o KV chats:allowed).
  Desconocido → requestAccess al admin con botones acc:ok/acc:no. approveChat
  agrega al KV y avisa a ambos. Cuando publica un operador, el admin recibe
  resumen con el link.
- Webhook re-registrado (action=set) — el bot vuelve al servicio para catalogar;
  el cron post-venta SIGUE apagado.
- Verificado: node --check OK; POST sintético a /tg/webhook con /start del admin
  (mensaje nuevo llegó) y con chat desconocido 999999999 (disparó la solicitud
  de acceso al admin — se le dijo al usuario que la rechace, era de prueba).
- PENDIENTE de prueba real: la esposa manda /start → aprobarla → catalogar un
  producto de verdad de punta a punta (foto → publicar).

## 7. Handoff (contexto al 84%)
Sesión cerrada tras desplegar el catalogador. CHECKPOINT.md y HANDOFF.txt
reescritos con el estado completo. Pendiente al retomar: rechazar solicitud
de prueba (chat 999999999), prueba real foto→publicar, alta de la esposa.

---

## Sesión 8 (2026-07-03, tarde) — Primera publicación real + fixes

### GitHub Pages
- Notificación de deploy fallido en main (run 28680912675, commit del checkpoint
  de sesión 7). Error: "Deployment failed, try again later" en actions/deploy-pages@v4
  a los 5 s de crear el deployment. `gh run rerun --failed` falló igual.
- Un run NUEVO vía `gh workflow run deploy.yml` (workflow_dispatch) pasó completo.
  Causa: transitoria del lado de GitHub. El sitio (25225840rico.github.io/MLPU)
  respondió 200 todo el tiempo con la versión anterior.
- Aviso pendiente sin urgencia: actions apuntan a Node 20 deprecado.

### Prueba real de catalogado (flujo completo)
- Usuario rechazó la solicitud sintética del chat 999999999. OK.
- Usuario mandó fotos de un Matchbox 2021 Ford Bronco. El análisis IA corrió
  bien: título "Matchbox 2021 Ford Bronco Metal Lifetime 30125", $12.000,
  MLC3398 Vehículos en Miniatura, nuevo, descripción completa.
  (Atributo BRAND quedó "Hot Wheels" — error de la IA, pendiente corregir.)
- Publicar → ML rechazó: validation error.
- El wrangler tail no capturó el intento (se quedó pegado); se diagnosticó
  reproduciendo el payload contra POST /items/validate con el token de KV:
  * `item.listing_type_id.unavailable`: free NO disponible en MLC3398 para esta
    cuenta; solo gold_pro/gold_premium/gold_special/gold/silver/bronze
    (confirmado con /users/283388639/available_listing_types?category_id=MLC3398).
  * `requiresPictures` (solo porque la validación no llevaba fotos).
- Bug adicional visto en el tail: "🧹 Limpiar" → bulk deleteMessages falla
  completo si un id no es borrable (>48 h) → fallback de 100 deleteMessage
  individuales → "Too many subrequests" (~límite 50) → ni el mensaje de
  confirmación salía.

### Fixes desplegados
1. publisher.js `pickListingType(token, categoryId)`: GET
   /users/{userId}/available_listing_types?category_id=; userId derivado del
   sufijo del access_token (APP_USR-<app>-...-<user_id>; verificado = 283388639;
   /users/me NO sirve en ese endpoint: "callerId must be a number").
   Preferencia free → gold_special → gold_pro → primero disponible; fallback
   'free' si la consulta falla. createListing lo usa y loguea si no es free.
2. telegram-bot.js clearChat: lotes de 25 vía deleteMessages, si un lote falla
   cae a deleteMessage individual solo de ese lote, todo bajo presupuesto de 40
   llamadas; el mensaje "🧹 Chat limpiado" siempre sale.
   → Deploy 875c5983. Usuario reintentó → **PUBLICADO: MLC4146267638**
   (gold_special, activa, permalink articulo.mercadolibre.cl/MLC-4146267638-...).
3. Descripción: el POST /items/{id}/description falló silencioso (catch con log).
   Reproducido: 400 `item.description.type.invalid` "must be in plain text",
   referencias apuntando a los caracteres ✓ ▪ ━ ⭐ 📦 que genera la IA.
   * sanitizeDescription() en publisher.js: viñetas unicode → "-", separadores
     ━─═— fuera, whitelist de caracteres (letras con acentos, números,
     puntuación básica), colapso de espacios/saltos.
   * Prompt del agente visión/copy: ya no pide emojis ni viñetas unicode
     (viñetas con guion simple).
   * createListing ahora loguea el body del rechazo de descripción (antes se
     perdía).
   * La descripción del Bronco se subió a mano ya sanitizada → 201 OK.
4. Cantidad editable (pedido del usuario): draft.qty (default 1) en
   runCatalogAnalyze; botón "🔢 Cantidad" (cat:qty) en la preview; modo cat_qty
   acepta 1–9999; available_quantity = qty en createListing; preview y mensaje
   de éxito muestran el stock real.
   → Deploy final 398e82af. Commit 45f0b18 pusheado.

### Respuestas dadas al usuario (pedidos de esta sesión)
- Cantidad: implementado (botón en preview).
- Descripción/HTML: ML solo acepta plain_text; solución = sanitizado + prompt.
- Compartir con esposa: ella abre t.me/Pulicadorlibre_bot → /start → al admin
  le llega Aprobar/Rechazar (KV chats:allowed). Sin pasos extra.
- Sonido al publicar: la API de bots no permite sonidos custom; el mensaje
  "🎉 ¡Publicado!" ya dispara la notificación normal. Para un sonido distintivo:
  en Telegram, perfil del bot → Notificaciones → Sonido personalizado (se puede
  subir un audio corto). Nada que programar.

### Estado del tail
- El tail de wrangler dejó de escribir tras el flood de subrequests de las
  16:07; para monitoreo futuro relanzar `npx wrangler tail mlpu-proxy --format pretty`.

---

## Sesión 8b — Análisis IA v2 (commit 688f194, Worker 5650aedd)
- analyzeProduct reescrito: UN agente haiku (max_tokens 2048) con hasta 3 fotos
  en la misma llamada (antes 4 llamadas paralelas subiendo la misma imagen 4×).
  Prompt detallado: leer logos/etiquetas letra por letra (Matchbox ≠ Hot Wheels,
  marca = la impresa, null si no se lee), specs (escala/material/color/serie),
  título+precio+searches coherentes con la misma identificación.
- fillAttributesWithAI: override duro post-IA — si analysis.brand existe se
  fuerza en BRAND y en todo atributo cuyo nombre contenga "marca"; opciones
  pasadas suben de 20 a 60; regla de omitir atributos indeterminables.
- Paralelizado: descarga de fotos del análisis (Promise.all), domain_discovery
  (3 búsquedas a la vez), getRequiredAttrs+fillAttributes ∥ getMarketPrices,
  y descarga+subida de fotos en runCatalogPublish (orden preservado, portada =
  primera foto).
- draft ahora guarda product/brand/model/features/specs → recategorizar
  (cat:cats) ya no pierde la marca.
- Pendiente de verificar con el próximo catalogado real.

---

## Sesión 8c — Precios 990, envío configurable, ganancia (commit 5e02839, Worker a7b265e8)
- roundTo990() en publisher.js: precio IA, mediana de mercado y ediciones
  manuales siempre terminan en 990 (mín 990). Testeado con 8 casos.
- Precio inicial del borrador = mediana de mercado redondeada (si hay avisos);
  fallback = estimación IA.
- draft.freeShipping (default false) + botón "🚚 Cambiar a envío…" (cat:ship)
  en la preview; createListing manda free_shipping según eso.
- estimateProfit(token, draft): comisión real vía /sites/MLC/listing_prices
  (CON token; sin token da 403 PolicyAgent) + costo envío vendedor vía
  /users/{id}/shipping_options/free con dimensions=10x10x10,500 (ML exige
  dimensiones pre-publicación; es estimación). Línea "💰 Ganancia estimada"
  en la preview (precio − comisión − envío si vendedor).
- Sin sale_terms de garantía (se quitó "Garantía del vendedor 30 días").
- Pregunta del usuario sobre medir mercado/tendencias por categoría:
  respondida (factible: /trends/MLC, /highlights, search por categoría);
  NO implementado, era solo consulta.

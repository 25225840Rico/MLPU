# 2026-07-16 — Sesión 14: post-mortem duplicados IG + fix anti-duplicación

## Reporte del usuario
"Ayer intenté publicar varios y no se pudo, se repetían." Pidió: más simple pero
igual de funcional, foco en IG, más robusto, mejor organizado.

## Evidencia (D1 remota + Graph API /media)
- Filas 2 y 3 en `error` (3 intentos, "Media ID is not available"), sin media_id.
- 8 publicaciones entre 02:08 y 02:43 UTC (22:08–22:43 Chile) → varias corridas
  seguidas de /ig ahora (la ventana del cron solo corrió a las 20:00, `ultima_corrida`).
- **Nissan GT-R (id 10) y Mazda RX-7 (id 11)**: el media_id del feed real ≠ el
  guardado en la DB → se publicaron DOS veces (el usuario borró una copia a mano).
- **Dodge Viper (id 12)**: publicado en el feed (02:43:07, media 18156971635487384)
  pero fila `pendiente` con 0 intentos → se habría duplicado en la próxima ventana.
- Tundra (id 4): DB dice publicado 02:08 pero no está en el feed (borrado a mano).

## Causas raíz (3)
1. **Sin idempotencia feed/historia**: la fila se marcaba `publicado` solo si
   feed Y historia salían. Historia fallaba ("Media ID is not available") → fila
   quedaba `pendiente` → el reintento volvía a subir el MISMO feed (hasta 3 veces).
2. **Sin lock ni claim**: /ig ahora corre en background (waitUntil); mandarlo dos
   veces (o cruzarse con el cron) hacía que dos corridas leyeran las mismas filas
   pendientes y publicaran duplicado. Si el Worker moría después del feed y antes
   del UPDATE, la fila quedaba `pendiente` con el post ya en IG (caso Viper).
3. **Poll insuficiente**: waitForContainer esperaba máx 15 s (25×600 ms); las
   imágenes compuestas (blur 1080×1920) tardan más → media_publish con el
   contenedor aún procesándose → "Media ID is not available".

## Fix (commit cd8d257, deploy 0d4712e2)
- `claimNext`: UPDATE…RETURNING atómico → estado `publicando` + intentos+1 +
  `claimed_en` (columna nueva, ALTER aplicado en remote).
- Feed idempotente: `ig_media_id` se guarda apenas sale; reintento lo salta.
- Historia best-effort: si falla, fila `publicado` + ultimo_error='historia: …'
  + aviso por Telegram ("solo feed"). Nunca más repite el feed.
- `recoverStuck`: `publicando` > 15 min → `pendiente` (sin repetir el feed).
- Lock consultivo `ig_config.corriendo` (TTL 10 min): segundo /ig ahora responde
  "ya hay una publicación en curso". El claim por fila es la garantía dura.
- Poll de Meta: 30×2 s (~60 s) + reintentos con base 3 s.
- `/ig ahora [n]`: publica hasta n (1-10, default 3).

## Datos corregidos en remote
- id 12 (Viper) → `publicado` con el media_id real del feed.
- ids 2 y 3 → `pendiente` intentos=0 (reintentarán con el poll paciente).
- Estado final: 134 pendientes · 10 publicados · 0 error. Pausa de debug quitada.

## Tests
46/46 (`npm test`); 6 nuevos: historia-falla-no-repite-feed, feed idempotente en
reintento, claim atómico, recovery de colgadas, lock enCurso, /ig ahora máx.
FakeDB extendido (RETURNING de claim, updates parciales, recovery).

## Parte 2 — goteo automático (commit af09e1d, deploy 60bd0749)
Usuario reportó: `/ig ahora 5` respondía "ya hay una en curso" y no pasaba nada.
Evidencia D1: corrida de 15:10 publicó id 2 OK (una sola vez — el fix funcionó),
reclamó id 3 y MURIÓ (~50 s: Cloudflare corta el background del webhook). Lock
pegado 10 min + fila en 'publicando'. Conclusión: tandas largas en un invoke NO
son confiables → goteo por cron.
- `/ig auto <min>` (mín 15) / `/ig auto off` / `/ig auto` (estado): el cron
  publica 1 producto cada <min> min, solo 09:00–23:00 Chile, ilimitado hasta
  vaciar la cola; avisa "cola vacía". Modo ventanas sigue si auto está off.
- Cron publisher */30 → */15 (goteo preciso; recovery de colgadas cada tick).
- Lock TTL 10→5 min. Aviso de enCurso explica que se libera solo.
- Datos: lock liberado, id 3 → pendiente. ACTIVADO EN PROD: modo_auto 90 min
  (Meta limita ~25 posts por API/24h; 90 min ≈ 9 ítems/día = ~19 contenedores).
- Tests 46→51 (goteo: 1 por tick, horario, intervalo, aviso vacía, off).
- Verificación en curso: tarea background consulta D1 tras el tick de ~16:45 UTC
  (primer goteo esperado: id 3 Matchbox Bronco).

## Pendiente
- Confirmar primer goteo real (~12:45 Chile) y que lleguen avisos por Telegram.
- SIGUE PENDIENTE (sesión 13): rotar App Secret en panel Meta y
  `wrangler secret put META_APP_SECRET`.
- Duplicados ya borrados a mano por el usuario en IG; Tundra (id 4) quedó sin
  post en el feed aunque la DB diga publicado (decidir si re-publicarlo).

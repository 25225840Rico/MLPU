# 2026-07-14 — MLPU-Instagram: diseño, plan e implementación completa (Tasks 1-6)

## Contexto
El usuario pidió (sesión iniciada 2026-07-13): usar el sistema para publicar el
inventario en Instagram (historias + feed con precio) automáticamente a la mejor
hora del día, y una web propia con checkout en su dominio. Decisiones vía
AskUserQuestion:
- Web = TIENDA PROPIA con checkout (MercadoPago Checkout Pro + Webpay Plus, ambos),
  sync bidireccional de stock con ML, dominio ya comprado en otro registrador
  (apuntar nameservers a Cloudflare). → Sub-proyecto 2, PENDIENTE.
- Instagram: automático al publicar, feed + historia AMBOS programados a la mejor
  hora según insights (online_followers), foto tal cual con precio en el caption,
  cuenta IG Business ya vinculada a página FB, app Meta YA CREADA por el usuario.
- OPTIMIZER CONGELADO a pedido del usuario (docs quedan en optimizer/docs/).
- Nuevo pedido: /ig stock para encolar el inventario ACTIVO ya publicado en ML.

## Documentos
- Spec: docs/superpowers/specs/2026-07-13-mlpu-instagram-design.md
  (commits 9627784, 03fd06c, + ajuste rate-limit YAGNI y /ig stock).
- Plan 7 tasks TDD: docs/superpowers/plans/2026-07-13-mlpu-instagram.md (b5a57f2, 9eb67e4).

## Implementación (rama feature/mlpu-instagram, ejecución inline)
- Task 1 (64241d4): npm test = `node --test "worker/test/*.test.js"` (glob:
  en Windows el path de directorio no resolvía). worker/schema-ig.sql
  (ig_queue: ml_item_id UNIQUE, estado pendiente|publicado|error|cancelado,
  intentos, ig_media_id/ig_story_id; ig_config clave/valor JSON). D1 creada:
  mlpu-db id 5145a70c-1c25-4904-a63a-4021b3bd19db, esquema aplicado local+remoto,
  binding env.DB.
- Task 2 (d18ab22): worker/ig-logic.js — fmtCLP, buildCaption (🔧/💰/👉 + HASHTAGS),
  pickBestWindows (top-2 horas, separación mínima 2h, circular; fallback si sin
  datos), isInWindow/windowKey (Intl en America/Santiago, h23; ventana 60 min).
- Task 3 (d3fd453): worker/ig-api.js — getMetaToken (D1), igPublishImage
  (contenedor → media_publish; STORIES sin caption), fetchOnlineFollowers
  (suma días; null si sin datos), maybeRefreshMetaToken (>45 días,
  fb_exchange_token, guarda en D1). Tests con stub de globalThis.fetch +
  worker/test/fake-db.js (FakeDB matchea SQL por fragmento).
- Task 4 (3582577): worker/ig-queue.js — enqueueIg (INSERT OR IGNORE),
  listPendientes, quitarDeCola (meta.changes), getVentanas (manual > insights >
  fallback), runIgPublisher (windowKey + ig_config.ultima_corrida = 1 corrida
  por ventana; LIMIT 3; verifica ítem activo vía /items/{id} con token ML —
  import DINÁMICO de index.js para no arrastrar el worker a los tests; foto =
  pictures[0].secure_url; feed+story; 3 intentos → error + notify), enqueueStock
  (paginado items/search status=active + multiget /items?ids= de a 20, filtra
  activos, dedupe, devuelve {total, encolados}), runIgDaily (ventanas + token,
  avisos en fallo). deps inyectables {getItem, publishImage, getToken, mlFetch}.
- Task 5 (045aa8c): telegram-bot.js — enqueueIg tras createListing en
  runCatalogPublish (con línea "📸 En cola para Instagram" en el mensaje de
  publicado, filter(Boolean)), handleIgCommand (/ig stock|cola|quitar|ahora|
  horas [HH:MM…|auto]), dispatcher `/ig`, ayuda en sendStart.
- Task 6 (8b50be0): index.js scheduled() rutea por event.cron ('0 10 * * *' →
  runIgDaily; resto → runIgPublisher); se quitó import de runScheduled
  (post-venta SIGUE apagado adrede). wrangler.toml: crons ["*/30 * * * *",
  "0 10 * * *"], var IG_USER_ID="PENDIENTE_TASK_7". Deploy OK versión 09ff897e
  (binding DB visible, 2 schedules).
- 22/22 tests en verde en cada paso.

## Task 7 PENDIENTE (interactiva)
Secrets META_APP_ID/META_APP_SECRET; IG_USER_ID + token largo vía Graph API
Explorer (pasos exactos en el plan y CHECKPOINT); sembrar meta_token en D1
remota; /ig stock + /ig ahora y verificar en la cuenta; luego
finishing-a-development-branch (merge a main) y SAVE.txt.

## Notas técnicas
- El cron 30-min corre SIEMPRE pero sale al tiro si no hay ventana activa
  (barato). Las historias por API no aceptan caption (solo imagen).
- Límite IG API 50 posts/día: inalcanzable con tope 3×2 ventanas (spec YAGNI).
- Tienda web (sub-proyecto 2): decisiones ya tomadas — extender Worker+D1,
  MercadoPago Checkout Pro + Webpay Plus, sync bidireccional (venta web pausa
  ML vía proxy /ml/; orden ML marca vendido en web), catálogo público servido
  por el Worker en el dominio del usuario (nameservers → Cloudflare).

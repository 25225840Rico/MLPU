# 2026-07-15 — Sesión 13: Task 7 MLPU-Instagram (setup Meta)

## Contexto
Reanudación desde CHECKPOINT. Usuario entregó App ID (2257279951741292) y App Secret
de la app Meta "topwheelsapp" directamente en el chat.

## Hecho
1. **Secrets**: `printf <valor> | npx wrangler secret put META_APP_ID / META_APP_SECRET`
   en worker/ → ambos OK en mlpu-proxy.
2. **Token corto**: el usuario lo generó en Graph API Explorer con los 5 permisos
   (instagram_basic, instagram_content_publish, instagram_manage_insights,
   pages_show_list, pages_read_engagement) y lo pegó en el chat.
3. **Hallazgo**: `GET /me/accounts` devolvió `{"data":[]}` pese a permisos granted.
   Solución: `GET /debug_token` (con app token `id|secret`) expone `granular_scopes`
   con los target_ids reales:
   - pages_show_list → page **660949953775866** ("TopWheels - Subasta -Venta y permuta
     de Hotwheels, Matchbox 1/64 Chile")
   - instagram_* → IG user **17841476463162844**
   Verificado: `GET /660949953775866?fields=instagram_business_account` → 17841476463162844;
   `GET /17841476463162844?fields=username` → **@topwheels.cl** (287 followers).
   Usuario del token: "Pickup Cl" (id 122334021944010749).
4. **Token largo**: `GET /oauth/access_token?grant_type=fb_exchange_token&client_id=…&client_secret=…&fb_exchange_token=…`
   → token de 200 chars (~60 días). Guardado temporalmente en scratchpad, luego borrado.
5. **Seed D1**: script Python generó `REPLACE INTO ig_config (clave, valor) VALUES
   ('meta_token', '{"token":"…","obtenido_en":"<ISO>"}')` → `wrangler d1 execute mlpu-db
   --remote --file … -y` → rows_written 2. SELECT de verificación OK.
   Nota: `ultima_corrida` ya existía con key 2026-07-15T12:30 → el cron */30 está corriendo
   y marcando ventanas (antes del token no tenía nada que publicar).
6. **wrangler.toml**: `IG_USER_ID = "17841476463162844"` (antes PENDIENTE_TASK_7).
7. **Deploy**: `npx wrangler deploy` → Version **65bcbb83-e662-4259-8a2e-eb94b4235bf7**,
   bindings verificados (env.DB mlpu-db, IG_USER_ID real, crons */30 y 0 10 UTC).
8. **Verificación token largo**: GET al IG user con el token largo → username/followers OK.
9. **Limpieza**: borrados lltoken.json y seed.sql del scratchpad.
10. **Commit**: 0957051 "Task 7: IG_USER_ID real de @topwheels.cl en wrangler.toml"
    en feature/mlpu-instagram (sin push).

## Incidencias
- Un Bash con heredocs anidados quedó colgado 2 min (timeout); se rehízo con un solo heredoc.

## Pendiente inmediato
- Prueba real (usuario en Telegram): /ig stock → /ig cola → /ig ahora → verificar
  feed+historia en @topwheels.cl. Si proporción rechazada → padding WASM.
- Luego: finishing-a-development-branch (merge a main + push) y actualizar SAVE.txt.
- Seguridad: rotar App Secret en panel Meta (quedó en el chat) y re-subir el secret.

## Parte 2 de la sesión (post-merge, iteración con el usuario en vivo)
- Fix "Media ID is not available": retry de media_publish con backoff (~15 s) mientras
  Meta procesa el contenedor; "not available" excluido de esErrorDeImagen.
- Cache-buster ?v= en /ig promo (Telegram cachea fotos por URL).
- Promo corregida: "ENTREGAS PRESENCIALES / Oficina La Poderosa 175 · Antofagasta".
- FONDO BLUR (spec Parte 5): compositor WASM photon en GET /ig/img?u=&s=1
  (worker/ig-image.js, solo mlstatic, caché 1 día). Cadena: blur → wsrv blanco → original.
  Verificado en prod: feed 1.6s / story 0.9s, CPU OK.
- Caption v2 SIN link de ML (compra por DM), hashtags corregidos a hotwheels/matchbox.
- Banner "DISPONIBLE" (pill verde, disponible.png vía env.ASSETS) estampado en historias.
- LÍMITE: la Graph API no permite stickers de link en historias (solo manual en la app).
- 40/40 tests · deploy ad87c68c · main d74cbd1 pusheado.

# Log 2026-06-18 (sesión 2) — Debug + botonera + historial

## Debug "no funciona"
- Verificado en vivo: webhook OK, comandos recibidos (logs [ML-BOT]), token ML activo,
  Anthropic key OK, chat alcanzable.
- BUG REAL: las respuestas del bot iban en `ctx.waitUntil` y el 200 cerraba el evento antes
  de completar el sendMessage → no llegaban. FIX: handleTelegramWebhook ahora `await work`.
  Desplegado; usuario confirmó que el bot responde.
- "/ordenes no funciona" = KV vacío (respondía "No hay órdenes", correcto). Sembrada orden
  de prueba TEST-1001 para demostrar; luego BORRADA a pedido del usuario.

## Botonera + cron
- ReplyKeyboard MAIN_KB + inline callback_data. Agregado 📚 Historial (KV, paginado),
  botones 🔎 por orden, detalle con 💬 Mensajear, flujo de despacho 100% botones
  ([✅ Sí][❌ No], [🏷 elegir orden], [✅ Enviar]). Estado pending await_message.
- Cron del scheduler cambiado de 6h a cada 10 min (*/10 * * * *).

## Hallazgo crítico ML 403
- GET /orders/{id} y /orders/search → 403 PolicyAgent (token sin permiso de órdenes).
- /users/me, /applications → OK. Callback de notificaciones bien configurado (verificado).
- Backfill de historial real IMPOSIBLE hasta re-autorizar la app (scope read). Seller id=283388639.

## Módulo 1 (historial real) — en curso
- CREADO worker/ml-history.js (compila): getOrderHistory, getOrdersByDateRange (pagina+delay),
  searchOrderByBuyer, getOrderDetail, getSellerId, todayRange/monthRange. Resuelve nombre
  comprador con GET /users/{id} (ME2), fallback nickname en 403. Log [ML-HISTORY].
- Mostrado el DIFF exacto para telegram-bot.js; usuario aprobó aplicarlo.
- PENDIENTE: aplicar diff + deploy (ver HANDOFF.txt, sección PROXIMO PASO EXACTO).

## Próximo
Aplicar diff Módulo 1 → deploy → [vars] SELLER_ID/ML_SITE_ID → Módulo 2 (ml-messaging.js).
Re-autorización ML pendiente del usuario para destrabar todo lo de órdenes.

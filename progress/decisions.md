2026-06-03 - Tema blanco con azul #2563eb como primario - El amarillo ML (#ffe234) era demasiado agresivo como color de acción; se usa solo para branding. El azul mejora legibilidad y contraste en mobile.
2026-06-03 - Borradores comprimidos a 480px (no full res) - localStorage tiene límite de 5MB; guardar imgs full podría saturarlo con pocos borradores.
2026-06-03 - publishBatch() secuencial, no paralelo - ML puede rechazar publicaciones simultáneas del mismo usuario; secuencial es más seguro y permite mostrar progreso real.
2026-06-03 - exchangeCode() via proxy existente - No requiere cambios al Cloudflare Worker; POST /ml/oauth/token ya está cubierto por la ruta /ml/* del proxy.
2026-06-04 - BUG-01 revertido (saltar ONBOARDING) - Saltarse ONBOARDING cuando hay token en LS causaba token expirado → comisiones $0 → margen 100%. ONBOARDING es checkpoint de validación; no saltar.
2026-06-04 - cropSquareForML() como safety net en publish, no en UI - El recorte visible lo hace CropScreen; cropSquareForML es fallback silencioso para subidas sin recorte previo.
2026-06-04 - CropScreen insertado entre captura y PREVIEW para todos los orígenes - Cámara, FullscreenCamera y archivos importados pasan por CROP; usuario puede omitir si no quiere recortar.
2026-06-04 - CropScreen forzado a solo ratio 1:1 - ML siempre muestra fotos en cuadrado; ofrecer otros ratios confundía al usuario porque el recorte automático posterior (cropSquareForML) quedaba mal encuadrado sin que el usuario lo controlara.
2026-06-04 - fillAttributesWithAI recibe extra={title,description,categoryName} - El prompt con solo brand/model/condition producía valores genéricos o incorrectos; más contexto reduce errores en atributos requeridos y baja las preguntas de compradores.
2026-06-04 - Stock default 3 (antes 1) + hint visual - ML penaliza publicaciones con stock 1; el default bajo pasaba desapercibido y el vendedor perdía ventas al agotar stock rápido.
2026-06-04 - Token ML vive en Cloudflare Worker KV, no en localStorage de cada celular - Dos usuarios en la misma cuenta ML se pisaban al renovar token en paralelo; KV centralizado elimina el conflicto y ningún celular ve el token en texto plano.
2026-06-04 - Client Secret se mueve al Worker como secret de Wrangler, no en frontend - Exponerlo en localStorage o código JS era un riesgo de seguridad; el Worker lo guarda cifrado en Cloudflare.
2026-06-04 - Migración IIFE al cargar app para URL Worker vieja→nueva - Evita que usuarios con localStorage viejo sigan apuntando a broad-pond sin tener que hacer nada manual.
2026-06-04 - Descripción ML en 2 pasos (POST /items sin description, luego POST /items/{id}/description) - API ML rechaza description dentro del POST /items desde 2021; separarlo evita errores silenciosos de publicación.
2026-06-04 - cleanTitle() aplica reglas duras ML antes de publicar - Títulos con símbolos o palabras prohibidas (envío gratis, nuevo, usado) causan moderación automática en ML; mejor limpiarlos en cliente.
2026-06-04 - max_tokens orchestrator 500→1024 - Descripción persuasiva de 150-400 palabras se truncaba con límite anterior; costo marginal justificado por calidad del output.
2026-06-04 - Modo automático engancha en onCrop (no en capture) - Mantiene CropScreen visible para que el usuario confirme el encuadre; saltar CropScreen completamente sería demasiado agresivo para el flujo.
2026-06-19 - Nunca usar `return void tgSend(...)` en el bot - El operador `void` descarta la promesa del envío; el Worker responde 200 (await work) y corta el fetch a Telegram en vuelo → mensajes racy/perdidos sin error en logs. Usar siempre `return tgSend(...)` o `await tgSend(...)`.
2026-06-19 - Toda lectura de ML tolera 429 via reintento en mlGet - El bot dispara ráfagas de lecturas (resolver nombres de compradores en paralelo) que dejan la cuenta rate-limited. mlGet reintenta con backoff (Retry-After, 429/503) y attachReceiverNames se escalona para no gatillarlo.
2026-06-19 - Mensaje "en preparación" al confirmar el pago (sin CTA) - Es el 1er mensaje de la venta; al ser informativo y cálido suele provocar respuesta del comprador, lo que desbloquea el cupo de inicio de ML (cap OTHER=1) para los mensajes siguientes (en camino / reseña+IG). Pedir reseña/IG recién al ENTREGAR, cuando tiene sentido.
2026-06-19 - Etapa "en preparación" gateada por recencia (≤5 días) - Evita que la primera corrida del cron tras el deploy mande "tu pedido está en preparación" a órdenes pagadas viejas que ya estaban en el KV.
- 2026-07-03 - Bot fuera de servicio de forma REVERSIBLE (borrar webhook + crons=[], sin tocar Worker/secrets/KV) - permite reactivar con un deploy + action=set; /ml/notifications sigue alimentando el KV.
- 2026-07-03 - Cambio de envío gratis→pagado aplicado a TODO el inventario vía API (elección del usuario), aceptando que ML rechace las ≥ $19.990 - ML ignora el cambio en silencio (200) en publicaciones con envío gratis obligatorio; sin daño en las rechazadas.
2026-07-03 - listing_type automático (free → gold_special → gold_pro) consultando available_listing_types por categoría - free no existe en todas las categorías (MLC3398 solo gold_*); ML rechazaba la publicación; se acepta comisión de Clásica cuando no hay gratuita.
2026-07-03 - Descripciones SIEMPRE sanitizadas a texto plano (sin emojis/símbolos) antes del POST /description - ML devuelve 400 "must be in plain text" con los caracteres ✓ ▪ ━ ⭐ que generaba la IA; además el prompt ya no los pide.
2026-07-03 - Stock deja de ser fijo en 1: default 1 pero editable desde la vista previa (botón 🔢 Cantidad) - pedido del usuario; hay productos con más de una unidad.
2026-07-03 - Sonido de publicación: no se programa en el bot; se configura sonido custom del chat en el cliente Telegram - la Bot API no soporta sonidos personalizados.
2026-07-08 - Catalogado por lotes: 1 álbum de Telegram = 1 producto; cada foto en su propia clave KV (lotp:<chat>:<lote>:<msgId>, file_id en metadata) - El pending único por chat con read-modify-write se corrompía con los updates simultáneos de un álbum y mezclaba modelos; claves individuales + ids de lote determinísticos (media_group_id) eliminan la carrera y permiten análisis/publicación en paralelo por producto.
2026-07-08 - Separación por producto vía pasada de visión previa (clusterPhotos) y sub-lotes con Analizar propio, en vez de un solo mega-análisis - El usuario manda fotos mezcladas de varios autos; una llamada barata de agrupación decide cuántos productos hay, y cada análisis corre en su propia invocación del Worker para no superar los ~50 subrequests del plan free.
2026-07-10 - Publicaciones SIEMPRE en la máxima categoría disponible (gold_pro Premium → gold_special → free) y envío SIEMPRE pagado por el comprador (sin toggle) - Pedido explícito del usuario; reemplaza la regla anterior de "listing_type más barato" y elimina draft.freeShipping y el botón cat:ship del bot; en el SPA solo cambió el default (selector manual sigue). Fallback ante fallo de available_listing_types: gold_pro.
2026-07-13 - MLPU-Instagram: publicación programada por ventanas (cola D1 + cron 30min + insights online_followers con fallback 12:30/20:00) en vez de publicar al instante - El usuario pidió "subir a la mejor hora del día"; una corrida por ventana con tope 3 evita spamear el perfil y hace inalcanzable el límite de 50 posts/día de la Graph API.
2026-07-13 - Token de Meta en D1 (ig_config.meta_token) con renovación automática fb_exchange_token a los 45 días - Un Worker no puede escribir sus propios secrets; D1 permite que el cron diario rote el token sin intervención.
2026-07-13 - /ig stock: carga inicial de todo el inventario activo de ML a la cola de IG (goteo ~6/día) - El usuario quiere partir promocionando el stock existente (160 ítems) sin esperar publicaciones nuevas; el goteo da ~1 mes de contenido.
2026-07-13 - Optimizer CONGELADO a pedido del usuario; la D1 mlpu-db (planeada para optimizer) nace en MLPU-Instagram - Prioridad: Instagram ya, tienda web después.

2026-07-15 - Page ID e IG_USER_ID se obtienen de debug_token→granular_scopes, no de /me/accounts - /me/accounts devolvió vacío pese a permisos granted (comportamiento de permisos granulares de Meta); debug_token con app-token expone los target_ids reales.
2026-07-17 - Limpieza s15: runScheduled (post-venta) se CONSERVA aunque nada lo invoque - esta apagado adrede y es reactivable; no es codigo muerto. App.jsx no se refactoriza a fondo (SPA legacy activo sin tests; riesgo > beneficio). Datos .xlsx/.csv de la raiz se ignoran en git pero no se borran (son del usuario). Politica de retry ML unificada: todo pasa por mlFetch.
2026-07-17 - Fallback de historias con compositor propio en modo pad (fondo blanco, m=pad) ANTES de wsrv - el banner DISPONIBLE solo lo estampa el compositor propio; con la cadena vieja (blur->wsrv->original) cualquier fallo del blur publicaba la historia sin banner. Lienzo blanco via constructor raw_pixels de PhotonImage (sin assets ni APIs nuevas). Feed sin pad propio (no lleva banner).
2026-07-17 - Blur de fondo OBLIGATORIO en publicaciones IG - se eliminan los fallbacks wsrv/URL original (salian sin blur); cadena = compositor blur -> blur lite (ambos propios); si fallan, la fila reintenta. Pedido explicito del usuario.
2026-07-17 - Rehistorias reutiliza la idempotencia existente (ig_media_id conservado = feed no se repite) en vez de un flag solo_historia - cero cambios en publicarFila salvo el aviso; prioridad por likes+comentarios del feed en columna nueva ig_queue.prioridad.
2026-07-19 - Rebaja -35% SOLO en ig_queue (IG), ML intacto; redondeo = precio*0.65 truncado a la DECENA - precios "chilenos" legibles (23990→15590); reversión exacta vía backup 2026-07-19-ig-precola, no ÷0.65.
2026-07-19 - Retiro de ítems de IG = estado 'publicado' + ig_media_id/ig_story_id NULL + nota en ultimo_error - único estado que NINGÚN flujo re-publica (publisher solo toma pendientes, rehistorias exige media id, enqueueStock solo resucita canceladas); aplicado a los 3 Nissan 370Z (ids 19/35/72).
2026-07-19 - Borrado de media IG vía API queda condicionado a regenerar el token Meta con instagram_manage_contents - fb_exchange_token no agrega scopes; verificación previa con /me/permissions y GET del media (caption debe matchear) antes de cualquier DELETE.

HANDOFF - MLPU (bot ML + Instagram) - 2026-07-16, sesion 14 (corte por contexto)

OBJETIVO GENERAL: bot Telegram (t.me/Pulicadorlibre_bot) sobre Cloudflare Worker
(mlpu-proxy) que cataloga y publica autos 1:64 en MercadoLibre y re-publica el
inventario en Instagram @topwheels.cl (feed con fondo blur + historia con banner
DISPONIBLE), con cola en D1 (mlpu-db, tabla ig_queue).

ESTADO ACTUAL (todo desplegado y pusheado):
- Repo: C:\Users\aronr\OneDrive\Documentos\PROYECTOS\automl (main 4dc6238
  pusheado; codigo del rush en 1423030). Worker Version d4c96a82. 56/56 tests.
- Esta sesion arreglo los DUPLICADOS del 15/07 (3 causas: sin idempotencia
  feed/historia, sin lock/claim entre corridas, poll de Meta de solo 15 s) con:
  claim atomico por fila (estado 'publicando' + columna claimed_en, migrada en
  remote), ig_media_id guardado apenas sale el feed (reintento nunca lo repite),
  historia best-effort, lock TTL 5 min, recovery de 'publicando' >15 min,
  poll ~60 s. Ademas /ig auto (goteo) y /ig rush.
- MODO RUSH ACTIVO en prod (ig_config.modo_auto={rush:true}): el cron */15
  publica 3 productos por tick, 09:00-23:00 Chile, consultando el cupo REAL de
  Meta en vivo (content_publishing_limit: quota_total=100/24h, las historias
  CUENTAN; reserva 4 para /ig promo). Cupo lleno -> UNA alerta con hora de
  reapertura (post mas viejo de la ventana +24h) y retoma solo.
- Cola al momento del corte: ~130 pendientes, 14+ publicados, 0 error.

ULTIMO PASO COMPLETADO: deploy d4c96a82 + activacion del rush (15:35 UTC) +
commits/push (codigo 1423030, progress 4dc6238). Quedaron 2 verificaciones
background de esta sesion que se pierden con el /clear (no importa).

PROXIMO PASO EXACTO: verificar que el rush corre solo. Comprobar:
1) En Telegram deben estar llegando avisos "Subido a IG" de a 3 cada 15 min
   (primer tick esperado: 15:45 UTC del 16/07). Preguntar al usuario o revisar.
2) En D1:  cd worker && npx wrangler d1 execute mlpu-db --remote --command
   "SELECT estado, COUNT(*) FROM ig_queue GROUP BY estado"  -> pendientes
   bajando, sin filas 'publicando' con claimed_en viejo, sin 'error'.
3) Cuando el cupo llegue a ~100/100 (hoy en la tarde), debe llegar la alerta
   "Cupo diario de Meta lleno" UNA sola vez, y manana el rush retoma solo.
Si algo fallo: revisar ultimo_error en ig_queue y logs con wrangler tail.

PENDIENTES DEL USUARIO:
- SEGURIDAD: rotar App Secret en panel Meta (quedo pegado en un chat el 15/07)
  y re-subir: cd worker && npx wrangler secret put META_APP_SECRET.
- Decidir si re-publicar la Tundra (ig_queue id 4): DB dice publicado pero el
  usuario borro ese post del feed a mano.
- Resto de pendientes heredados: ver progress/STATE.json open_tasks.

ARCHIVOS CLAVE:
- worker/ig-queue.js (cola, claim, rush, goteo) - worker/ig-api.js (Graph API,
  quota) - worker/ig-logic.js (puro: captions, horarios, horaChile)
- worker/telegram-bot.js handleIgCommand (/ig rush|auto|ahora|cola|...)
- worker/schema-ig.sql (estados y claimed_en) - worker/test/* (56 tests, npm test)
- progress/CHECKPOINT.md (resumen) - progress/log/2026-07-16.md (post-mortem
  completo de esta sesion con evidencia)

COMO RETOMAR: leer este archivo y ejecutar el PROXIMO PASO EXACTO. No hay
codigo a medio escribir; el arbol esta limpio y todo pusheado.

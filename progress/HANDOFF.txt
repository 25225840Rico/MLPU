HANDOFF — MLPU · Bot Telegram catalogador multi-operador — 2026-07-03 (sesión 7)

OBJETIVO
Reemplazar el SPA "ML AutoPublisher" (index.html + src/) por el bot de Telegram:
2 personas (usuario + esposa), cada una desde su celular, mandan fotos de
productos al bot y este publica en LA MISMA cuenta de MercadoLibre del usuario.
Reglas fijas: envío SIEMPRE a cargo del comprador, stock SIEMPRE 1.

REPO Y ENTORNO
- Repo: https://github.com/25225840Rico/MLPU (main). Trabajo directo en
  C:\Users\aronr\OneDrive\Escritorio\automl (ES el clon; worker/ = código vivo).
- Último commit: ce3a59e (pusheado). Worker Version: b67b83e1 (desplegado).
- Worker: https://mlpu-proxy.aronricocl.workers.dev
- Bot: t.me/Pulicadorlibre_bot · admin chat 1036420688 · seller 283388639 (MLC).
- KV: ML_TOKENS 1f6324c3659d4388bec284825c2864be (clave ml_session con OAuth) ·
  ML_ORDERS d72bf35159b54eabadffafc521c1562b (órdenes + chats:allowed +
  pending:<chatId> = borradores de catalogado, TTL 1h).
- Secrets del Worker (ya puestos): ANTHROPIC_API_KEY, ML_CLIENT_ID,
  ML_CLIENT_SECRET, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID.
- WEBHOOK ACTIVO (se reactivó con /tg/admin?action=set). CRON APAGADO
  (wrangler.toml crons=[]; el scheduler post-venta NO corre; decisión usuario).
- Herramienta PowerShell puede no estar; usar Bash (Git Bash). Python 3.14 con
  openpyxl instalado. wrangler kv mete "npm notice" en stdout (filtrar con grep).

ESTADO ACTUAL (todo desplegado y pusheado)
1. Bot post-venta fuera de servicio (cron []) pero el webhook fue REACTIVADO
   para el nuevo rol de catalogador. /tg/admin soporta info|set|off|catchup.
2. Inventario (149 publicaciones): 129 con envío pagado por comprador, 20 con
   envío gratis obligatorio (≥$25.990, ML no permite quitarlo; usuario decidió
   no rebajarlas). 20 items quedaron a $19.980. Stock 1 en 143 (5 en 0 a
   propósito; MLC1986993239 under_review no se pudo, sigue con 3).
   Excel: automl\inventario_ML_2026-07-03.xlsx.
3. NUEVO worker/publisher.js: analyzeProduct (4 llamadas paralelas a
   claude-haiku-4-5-20251001: visión/SEO/precio/categoría), discoverCategories
   (domain_discovery MLC), getRequiredAttrs + fillAttributesWithAI,
   getMarketPrices, uploadPicture (multipart /pictures/items/upload),
   createListing (listing_type free, garantía 30 días, shipping
   {mode:me2, free_shipping:false, local_pick_up:false}, available_quantity:1,
   descripción por POST /items/{id}/description aparte).
4. telegram-bot.js integrado: FOTOS → catalogado por defecto (acumula
   file_ids, botón "🤖 Analizar" = cat:go). Vista previa editable (cat_confirm):
   botones cat:pub / cat:price / cat:title / cat:cats (top 5, recalcula
   atributos) / cat:cond / cat:x. Si ML rechaza el alta, el borrador queda y
   se puede Reintentar. El flujo de sticker/tracking quedó tras el botón
   "📦 Despacho" (mode await_sticker). MAIN_KB nuevo con 📸 Catalogar.
5. Acceso multi-operador: isAllowed = admin (TELEGRAM_CHAT_ID) o lista KV
   chats:allowed. Chat desconocido → requestAccess al admin con botones
   acc:ok:<id> / acc:no:<id> (approveChat). Cuando publica un operador, el
   admin recibe aviso con link.
6. Verificado: node --check OK en telegram-bot.js y publisher.js; POST
   sintético a /tg/webhook con /start (llegó al chat real del admin) y con un
   chat desconocido (999999999) que disparó la solicitud de acceso.

ÚLTIMO PASO COMPLETADO
Commit ce3a59e pusheado, Worker desplegado (b67b83e1), webhook re-registrado,
pruebas sintéticas OK. Checkpoint guardado.

PRÓXIMO PASO EXACTO (al retomar, en este orden)
1. Preguntar al usuario si RECHAZÓ la solicitud del chat 999999999 (era prueba
   sintética mía; NO aprobarla).
2. PRUEBA REAL de punta a punta: que el usuario mande fotos de un producto al
   bot → 🤖 Analizar → revisar preview → ✅ Publicar. Monitorear con:
     cd worker && npx wrangler tail mlpu-proxy --format pretty
   Errores probables a ajustar en publisher.js/createListing: atributos
   requeridos faltantes (cause item.attributes.missing_required), categoría
   inválida, o formato de sale_terms según categoría.
3. Alta de la esposa: ella manda /start al bot → al admin le llega Aprobar →
   verificar que puede catalogar en paralelo (su pending:<chatId> es propio).
4. Si el usuario borra una publicación de prueba, recordar que se puede pausar
   en vez de borrar (PUT status paused).

ARCHIVOS CLAVE
- worker/publisher.js (pipeline IA + publicación; reglas fijas del negocio).
- worker/telegram-bot.js (webhook, acceso, flujo catalogado: buscar "Módulo 3",
  handlePhoto, runCatalogAnalyze, sendCatalogPreview, runCatalogPublish,
  isAllowed/requestAccess/approveChat; pending:<chatId> en ML_ORDERS).
- worker/index.js (router, getValidAccessToken, handleTgAdmin info|set|off|catchup).
- worker/wrangler.toml (crons=[] — NO reactivar el cron sin pedido del usuario).
- src/agents/orchestrator.js + src/App.jsx (SPA original, referencia; legacy).
- inventario_ML_2026-07-03.xlsx (snapshot del inventario).

CÓMO RETOMAR
- Leer este HANDOFF + progress/CHECKPOINT.md. No releer el historial.
- Estado vivo: curl "https://mlpu-proxy.aronricocl.workers.dev/tg/admin?action=info"
  (webhook debe apuntar a /tg/webhook).
- Logs en vivo: cd worker && npx wrangler tail mlpu-proxy --format pretty
- Continuar desde PRÓXIMO PASO EXACTO (prueba real + alta esposa).
